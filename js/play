

///////////////////////////////////REFACTOR THIS , changePopup(popup, changeState)

const changePopup = (popup) = { 
  popup.classList.contains('hide')? removeElement(popup, hide) : addingElement(popup, "hide");
   popup.classList.contains('popup-open')? removeElement(popup, popup-open) : addingElement(popup, "popup-open")

}